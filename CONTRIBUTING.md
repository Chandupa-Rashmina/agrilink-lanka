# Contributing

1. Do not develop directly on `agrilink-lanka-stable`.
2. Create a named working branch.
3. Make one focused change at a time.
4. Run relevant verification and tests.
5. Review `git diff` before committing.
6. Never commit secrets or production credentials.
